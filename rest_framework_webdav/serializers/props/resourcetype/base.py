from rest_framework_webdav.namespaces import DAVNS

class BaseResourcetypeChild(object):
    """
    Subclass `BaseResourcetypeChild` and a `Field` class to implement your own
    resource type.
    """
    namespace = DAVNS
